<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danxzzy - Layanan Digital</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #1a1a2e;
            --secondary-color: #16213e;
            --accent-color: #0f3460;
            --text-color: #e6e6e6;
            --highlight-color: #e94560;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: var(--text-color);
            min-height: 100vh;
            overflow-x: hidden;
            transition: background-color 0.5s ease;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Styles */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 30px;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            background: linear-gradient(45deg, var(--highlight-color), #ff7bac);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            display: flex;
            align-items: center;
        }

        .logo i {
            margin-right: 10px;
            font-size: 32px;
        }

        /* Navigation Styles */
        .social-nav {
            display: flex;
            gap: 15px;
        }

        .social-nav a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            color: var(--text-color);
            text-decoration: none;
            transition: all 0.3s ease;
            font-size: 18px;
        }

        .social-nav a:hover {
            transform: translateY(-5px);
            background: var(--highlight-color);
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 60px 20px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            margin-bottom: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(233, 69, 96, 0.1) 0%, rgba(0, 0, 0, 0) 70%);
            z-index: -1;
        }

        .hero h1 {
            font-size: 48px;
            margin-bottom: 20px;
            background: linear-gradient(45deg, var(--highlight-color), #ff7bac);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .hero p {
            font-size: 18px;
            max-width: 700px;
            margin: 0 auto 30px;
            line-height: 1.6;
        }

        /* Music Player */
        .music-player {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 40px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .player-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .player-header h2 {
            font-size: 24px;
            display: flex;
            align-items: center;
        }

        .player-header h2 i {
            margin-right: 10px;
            color: var(--highlight-color);
        }

        .playlist {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .playlist-item {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 15px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
        }

        .playlist-item:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateY(-3px);
        }

        .playlist-item.active {
            background: rgba(233, 69, 96, 0.2);
            border-left: 3px solid var(--highlight-color);
        }

        .playlist-item i {
            margin-right: 10px;
            color: var(--highlight-color);
        }

        .player-controls {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .player-controls button {
            background: var(--highlight-color);
            border: none;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .player-controls button:hover {
            transform: scale(1.1);
            box-shadow: 0 0 15px rgba(233, 69, 96, 0.5);
        }

        .progress-container {
            flex-grow: 1;
            background: rgba(255, 255, 255, 0.1);
            height: 6px;
            border-radius: 3px;
            margin: 0 15px;
            cursor: pointer;
            position: relative;
        }

        .progress {
            background: var(--highlight-color);
            height: 100%;
            border-radius: 3px;
            width: 0%;
        }

        .time {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.7);
        }

        /* Services Section */
        .services {
            margin-bottom: 40px;
        }

        .section-title {
            font-size: 32px;
            margin-bottom: 30px;
            text-align: center;
            position: relative;
            display: inline-block;
            left: 50%;
            transform: translateX(-50%);
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--highlight-color), transparent);
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .service-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            padding: 25px;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--highlight-color), #ff7bac);
        }

        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }

        .service-card h3 {
            font-size: 20px;
            margin-bottom: 15px;
            color: var(--highlight-color);
            display: flex;
            align-items: center;
        }

        .service-card h3 i {
            margin-right: 10px;
        }

        .service-card p {
            line-height: 1.6;
            color: rgba(255, 255, 255, 0.8);
        }

        /* Payment Section */
        .payment {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 40px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .payment-methods {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .payment-method {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 10px;
            padding: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .payment-method i {
            font-size: 32px;
            margin-bottom: 10px;
            color: var(--highlight-color);
        }

        .payment-method h4 {
            margin-bottom: 5px;
        }

        .payment-method p {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.7);
        }

        /* Background Changer */
        .bg-changer {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 100;
        }

        .bg-toggle {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--highlight-color);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 20px;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }

        .bg-toggle:hover {
            transform: scale(1.1);
        }

        .bg-options {
            position: absolute;
            bottom: 60px;
            right: 0;
            background: rgba(30, 30, 46, 0.9);
            border-radius: 10px;
            padding: 10px;
            display: none;
            flex-wrap: wrap;
            gap: 10px;
            width: 200px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .bg-option {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            cursor: pointer;
            border: 2px solid transparent;
            transition: all 0.3s ease;
        }

        .bg-option:hover {
            transform: scale(1.1);
        }

        .bg-option.active {
            border-color: white;
        }

        .bg-black { background: #1a1a2e; }
        .bg-white { background: #f5f5f5; }
        .bg-blue { background: #1e3a8a; }
        .bg-red { background: #b91c1c; }
        .bg-rgb { background: linear-gradient(45deg, #ff0000, #00ff00, #0000ff); }

        /* Telegram Message */
        .telegram-message {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 40px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .message-form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 20px;
        }

        .message-form input,
        .message-form textarea {
            background: rgba(255, 255, 255, 0.08);
            border: none;
            border-radius: 10px;
            padding: 15px;
            color: var(--text-color);
            font-size: 16px;
        }

        .message-form textarea {
            min-height: 120px;
            resize: vertical;
        }

        .message-form button {
            background: var(--highlight-color);
            border: none;
            border-radius: 10px;
            padding: 15px;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .message-form button:hover {
            background: #ff2e4f;
            transform: translateY(-3px);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 30px 0;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            margin-top: 50px;
            color: rgba(255, 255, 255, 0.7);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 36px;
            }
            
            .services-grid {
                grid-template-columns: 1fr;
            }
            
            .playlist {
                grid-template-columns: 1fr;
            }
            
            .payment-methods {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">
                <i class="fas fa-code"></i>
                Danxzzy
            </div>
            <nav class="social-nav">
                <a href="https://t.me/danxzzy" target="_blank"><i class="fab fa-telegram"></i></a>
                <a href="https://youtube.com/@dnxzvucx?si=pIJDvTqUKgOJYPVV" target="_blank"><i class="fab fa-youtube"></i></a>
                <a href="https://whatsapp.com/channel/0029Vb6nelkC1Fu5pX470F1d" target="_blank"><i class="fab fa-whatsapp"></i></a>
                <a href="https://www.tiktok.com/@dnxzvuck?_t=ZS-90bTvxO7OGe&_r=1" target="_blank"><i class="fab fa-tiktok"></i></a>
            </nav>
        </header>

        <section class="hero">
            <h1>Layanan Digital Terpercaya</h1>
            <p>Kami menyediakan berbagai layanan digital berkualitas dengan harga terjangkau. Dari pembuatan website hingga jasa banned WhatsApp, semua bisa Anda dapatkan di sini.</p>
        </section>

        <section class="music-player">
            <div class="player-header">
                <h2><i class="fas fa-music"></i> Musik Player</h2>
            </div>
            <div class="playlist" id="playlist">
                <!-- Playlist items will be added by JavaScript -->
            </div>
            <div class="player-controls">
                <button id="prev-btn"><i class="fas fa-step-backward"></i></button>
                <button id="play-btn"><i class="fas fa-play"></i></button>
                <button id="next-btn"><i class="fas fa-step-forward"></i></button>
                <div class="progress-container">
                    <div class="progress" id="progress"></div>
                </div>
                <div class="time" id="current-time">0:00</div>
                <div class="time" id="duration">0:00</div>
            </div>
        </section>

        <section class="services">
            <h2 class="section-title">Layanan Kami</h2>
            <div class="services-grid">
                <div class="service-card">
                    <h3><i class="fas fa-cogs"></i> Jasa Pasang Script</h3>
                    <p>Layanan pemasangan script custom sesuai kebutuhan Anda dengan hasil yang optimal.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-ban"></i> Jasa Banned WhatsApp</h3>
                    <p>Layanan banned WhatsApp dengan metode terbaru dan efektif.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-unlock"></i> Unbanned WhatsApp</h3>
                    <p>Bantuan unbanned akun WhatsApp yang terkena banned dengan tingkat keberhasilan tinggi.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-edit"></i> Jasa Rename Script</h3>
                    <p>Layanan mengganti nama script sesuai keinginan Anda dengan cepat dan aman.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-mobile-alt"></i> Jasa Rename Aplikasi</h3>
                    <p>Custom nama aplikasi sesuai brand atau keinginan Anda.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-globe"></i> Jasa Buat Website Domain</h3>
                    <p>Pembuatan website profesional dengan domain custom sesuai kebutuhan bisnis Anda.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-comment-alt"></i> Text Spam Tinjau Permanen</h3>
                    <p>Jual text spam untuk tinjau permanen dengan metode terbaru.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-key"></i> Text Unbanned</h3>
                    <p>Text khusus untuk proses unbanned akun dengan tingkat keberhasilan tinggi.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-video"></i> Video Tutorial Banned</h3>
                    <p>Video tutorial lengkap cara banned WhatsApp dengan metode terbaru.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-film"></i> Video Unbanned</h3>
                    <p>Video panduan lengkap cara unbanned akun WhatsApp yang terkena banned.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-bug"></i> APK Bug Orca Malignant</h3>
                    <p>Jual APK bug Orca Malignant dengan fitur lengkap dan update terbaru.</p>
                </div>
                <div class="service-card">
                    <h3><i class="fas fa-map-marker-alt"></i> APK Sadap Alamat</h3>
                    <p>APK untuk melacak alamat dengan akurasi tinggi dan fitur lengkap.</p>
                </div>
            </div>
        </section>

        <section class="payment">
            <h2 class="section-title">Metode Pembayaran</h2>
            <div class="payment-methods">
                <div class="payment-method">
                    <i class="fas fa-wallet"></i>
                    <h4>DANA</h4>
                    <p>Tidak tersedia</p>
                </div>
                <div class="payment-method">
                    <i class="fas fa-mobile-alt"></i>
                    <h4>OVO</h4>
                    <p>085711586592</p>
                </div>
                <div class="payment-method">
                    <i class="fas fa-credit-card"></i>
                    <h4>GOPAY</h4>
                    <p>085711586592</p>
                </div>
                <div class="payment-method">
                    <i class="fas fa-university"></i>
                    <h4>BANK</h4>
                    <p>Tidak tersedia</p>
                </div>
                <div class="payment-method">
                    <i class="fas fa-landmark"></i>
                    <h4>BCI</h4>
                    <p>Tidak tersedia</p>
                </div>
                <div class="payment-method">
                    <i class="fas fa-building"></i>
                    <h4>BCA</h4>
                    <p>Tidak tersedia</p>
                </div>
            </div>
        </section>

        <section class="telegram-message">
            <h2 class="section-title">Kirim Pesan ke Telegram</h2>
            <div class="message-form">
                <input type="text" id="name" placeholder="Nama Anda">
                <input type="email" id="email" placeholder="Email Anda">
                <textarea id="message" placeholder="Pesan Anda"></textarea>
                <button id="send-message">
                    <i class="fab fa-telegram"></i> Kirim Pesan
                </button>
            </div>
        </section>
    </div>

    <div class="bg-changer">
        <div class="bg-toggle" id="bg-toggle">
            <i class="fas fa-palette"></i>
        </div>
        <div class="bg-options" id="bg-options">
            <div class="bg-option bg-black active" data-color="black"></div>
            <div class="bg-option bg-white" data-color="white"></div>
            <div class="bg-option bg-blue" data-color="blue"></div>
            <div class="bg-option bg-red" data-color="red"></div>
            <div class="bg-option bg-rgb" data-color="rgb"></div>
        </div>
    </div>

    <footer>
        <p>&copy; 2023 Danxzzy. All rights reserved.</p>
    </footer>

    <script>
        // Music Player Functionality
        const audio = new Audio();
        const playlist = [
            {
                title: "7 Years",
                src: "https://7yyearsold.edgeone.app/7%20Years_musicaldown.com_1760775101.mp3"
            },
            {
                title: "Sekarang Hingga Nanti Kita Tua",
                src: "https://close-harlequin-fnlsj7oi63.edgeone.app/Sekarang%20Hingga%20Nanti%20Kita%20Tua_musicaldown.com_1760775036.mp3"
            },
            {
                title: "Original Sound - Not Ale Xander",
                src: "https://indirect-cyan-rarzlftoo2.edgeone.app/original%20sound%20-%20not_ale_xander_musicaldown.com_1760775073.mp3"
            },
            {
                title: "Original Sound - Karindingsadulur",
                src: "https://calm-moccasin-mms3vzvzh1.edgeone.app/original%20sound%20-%20karindingsadulur_musicaldown.com_1760774990.mp3"
            },
            {
                title: "Don't You Remember",
                src: "https://danxzzy.edgeone.app/Don%27t%20You%20Remember_musicaldown.com_1760774383.mp3"
            }
        ];

        let currentTrack = 0;
        let isPlaying = false;

        const playBtn = document.getElementById('play-btn');
        const prevBtn = document.getElementById('prev-btn');
        const nextBtn = document.getElementById('next-btn');
        const progress = document.getElementById('progress');
        const currentTimeEl = document.getElementById('current-time');
        const durationEl = document.getElementById('duration');
        const playlistEl = document.getElementById('playlist');

        // Create playlist items
        playlist.forEach((track, index) => {
            const playlistItem = document.createElement('div');
            playlistItem.className = 'playlist-item';
            if (index === 0) playlistItem.classList.add('active');
            playlistItem.innerHTML = `
                <i class="fas fa-music"></i>
                <span>${track.title}</span>
            `;
            playlistItem.addEventListener('click', () => {
                currentTrack = index;
                loadTrack(currentTrack);
                playTrack();
            });
            playlistEl.appendChild(playlistItem);
        });

        function loadTrack(index) {
            audio.src = playlist[index].src;
            document.querySelectorAll('.playlist-item').forEach((item, i) => {
                if (i === index) {
                    item.classList.add('active');
                } else {
                    item.classList.remove('active');
                }
            });
        }

        function playTrack() {
            isPlaying = true;
            audio.play();
            playBtn.innerHTML = '<i class="fas fa-pause"></i>';
        }

        function pauseTrack() {
            isPlaying = false;
            audio.pause();
            playBtn.innerHTML = '<i class="fas fa-play"></i>';
        }

        function nextTrack() {
            currentTrack = (currentTrack + 1) % playlist.length;
            loadTrack(currentTrack);
            if (isPlaying) playTrack();
        }

        function prevTrack() {
            currentTrack = (currentTrack - 1 + playlist.length) % playlist.length;
            loadTrack(currentTrack);
            if (isPlaying) playTrack();
        }

        function updateProgress() {
            const { duration, currentTime } = audio;
            const progressPercent = (currentTime / duration) * 100;
            progress.style.width = `${progressPercent}%`;
            
            // Format time
            const formatTime = (time) => {
                const minutes = Math.floor(time / 60);
                const seconds = Math.floor(time % 60);
                return `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
            };
            
            currentTimeEl.textContent = formatTime(currentTime);
            if (duration) {
                durationEl.textContent = formatTime(duration);
            }
        }

        function setProgress(e) {
            const width = this.clientWidth;
            const clickX = e.offsetX;
            const duration = audio.duration;
            audio.currentTime = (clickX / width) * duration;
        }

        // Event listeners
        playBtn.addEventListener('click', () => {
            if (isPlaying) {
                pauseTrack();
            } else {
                playTrack();
            }
        });

        nextBtn.addEventListener('click', nextTrack);
        prevBtn.addEventListener('click', prevTrack);
        audio.addEventListener('timeupdate', updateProgress);
        audio.addEventListener('ended', nextTrack);
        document.querySelector('.progress-container').addEventListener('click', setProgress);

        // Load first track
        loadTrack(0);

        // Background Changer
        const bgToggle = document.getElementById('bg-toggle');
        const bgOptions = document.getElementById('bg-options');
        const bgOptionsElements = document.querySelectorAll('.bg-option');

        bgToggle.addEventListener('click', () => {
            bgOptions.style.display = bgOptions.style.display === 'flex' ? 'none' : 'flex';
        });

        bgOptionsElements.forEach(option => {
            option.addEventListener('click', () => {
                const color = option.getAttribute('data-color');
                
                // Remove active class from all options
                bgOptionsElements.forEach(opt => opt.classList.remove('active'));
                // Add active class to clicked option
                option.classList.add('active');
                
                // Change background based on color
                switch(color) {
                    case 'black':
                        document.body.style.background = 'linear-gradient(135deg, #1a1a2e, #16213e)';
                        break;
                    case 'white':
                        document.body.style.background = 'linear-gradient(135deg, #f5f5f5, #e0e0e0)';
                        document.body.style.color = '#333';
                        break;
                    case 'blue':
                        document.body.style.background = 'linear-gradient(135deg, #1e3a8a, #1e40af)';
                        break;
                    case 'red':
                        document.body.style.background = 'linear-gradient(135deg, #b91c1c, #dc2626)';
                        break;
                    case 'rgb':
                        document.body.style.background = 'linear-gradient(135deg, #ff0000, #00ff00, #0000ff)';
                        break;
                }
                
                // Hide options after selection
                bgOptions.style.display = 'none';
            });
        });

        // Telegram Message
        document.getElementById('send-message').addEventListener('click', () => {
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            const message = document.getElementById('message').value;
            
            if (!name || !email || !message) {
                alert('Harap isi semua field!');
                return;
            }
            
            // In a real implementation, you would send this data to a server
            // that forwards it to Telegram via bot API
            const telegramUrl = `https://t.me/danxzzy`;
            window.open(telegramUrl, '_blank');
            
            // Reset form
            document.getElementById('name').value = '';
            document.getElementById('email').value = '';
            document.getElementById('message').value = '';
            
            alert('Pesan akan dikirim ke Telegram. Pastikan Anda sudah membuka aplikasi Telegram!');
        });
    </script>
</body>
</html>
